# Copier ce fichier en .env et adapter les valeurs
# (le .env reel n'est jamais commite : il est dans .gitignore)

NODE_ENV=production
PORT=3000

# Identifiants de la base de donnees PostgreSQL
DB_NAME=todo_db
DB_USER=todo_user
DB_PASSWORD=change_me
